# AI入力最小化の進捗・完了記録

最新状況：2026-09-17の追加対応で、稼働中n8nへの反映と旧保留の再開対応を完了。以下の初回記録は履歴として保持し、初回の「未反映」「missingTopicsなしは維持」の制限は末尾の追加対応で更新した。

2026-09-17：4フェーズ完了。代表6パターン合格。コミット・プッシュなし。

## フェーズ1：比較の準備（完了）

指示書・決定事項を一度確認し、作成済み準備文書を再作成しなかった。適用AGENTS.mdなし。変更前HEADは 488f6b9d040463fc21c00fab79f769d1d44239a1。既存の未コミット変更一覧と関係コードを .operation-state/ai-input-minimization/ に保存し、実装前に隔離DBの変更前入力（初回＋追加参照）を保存した。

通常sandboxとnode/browser操作ツールは setup refresh had errors で初期化失敗。承認されたexecの昇格経路からWSLと既存Chrome検証スクリプトを実行した。システム障害を検証合格には算入していない。

## フェーズ2：対象選定（完了）

scripts/ai_input_minimization.py に工程共通の選定を追加。確認済み成果物の永続保存、未完了、保留、取得不能を区別し、一覧・詳細・追加参照で保存済みを除外する。要約は当時のreview snapshot、提案は記事単位の保存receipt/既存の明示的なAI確認と歴史的根拠から判定。readyや空の下書きだけでは完了にしない。該当なしはreviewedArticlesで対象記事を明記する。

本文取得不能（unavailable/metadata_only）は期限到来済み予定・retry/refreshでも対象外とした。失敗履歴・理由を保持し、本文不要の人材工程は残す。保留はmissingTopicsと新しい事実/引用の一致だけで再開し、再保留の使用材料は別review履歴で保持する。除外件数と短い理由を返す。

## フェーズ3：入力・追加参照・保存の接続（完了）

inputVersion 2では工程別facts/entities/evidenceと短いrefを返す。初回確認時だけ保存本文を返す。taskFacts未指定の既存記録は、既存の人物対応などを使った保守的な集合とし、全件再分類しない。

refは対象日・工程・記事ID・occurrence・本文版・fetch・review・ルールに結び、既存のimmutable historyへ保存する。本文部分参照を流用し、detail/body/facts/evidence/sourceをDBから限定参照する。保存時にURL・記事タイトル・記事キー・人物関係キーを補完し、新規成果物の現在の根拠検証は維持する。既存成果物は当時の根拠で再利用し、追加保存で以前の成果物を落とさない。

既存のproject_business_writes / outbox / project_readersを利用し、スキーマ追加・本番DB移行は不要。停止済みの機械用JSON/Data Table自動反映は再開していない。成果物確認・progress・briefを接続し、固定ルール、運用開始手順、運用スキル、ローカルの生成指示元を更新した。過去日の生成指示書は書き直していない。

## フェーズ4：代表例・削減効果（完了）

tests/test_ai_input_minimization.pyの代表6ケースを隔離SQLiteで確認した。修正後は影響するケースだけ再確認した。

- 1. 新しい記事：合格。初回refによる本文確認と共通記録保存／要約・人材・分類のDB保存と読み出し／人物関係のarticleRef補完・既存DB反映経路／代表1件のChrome DOMと画像（記事・要約・人物・分類・正式出典・Project DB）。
- 2. 一部工程が保存済み：合格。要約だけの保存で他工程は残る／人材該当なしを記事単位で保存／本文・ルール変更後も保存済み工程を除外／過去の根拠と未反映提案を保持／既存要約/否定提案の再利用、進捗入力変更の無視。
- 3. 成果物が未保存：合格。readyだけでは除外しない／空の未確認提案で全記事完了にしない／DB書き込み中の失敗でrollbackし未完了を維持／未確認Markdownの保存を拒否。
- 4. 本文取得不能：合格。期限経過済みretry_after=1で翌日相当の再実行も除外／refresh/retry指定でも取得不能を再取得しない／保存失敗理由と取得履歴保持／本文不要の人材入力は残る。
- 5. 判断保留：合格。同じ材料・無関係topic・ルール更新では再開しない／不足topicに一致する新しい根拠だけで再開／再保留後は同じ材料で再開しない／4判断と4本文snapshotを保持。
- 6. 最小入力と追加参照：合格。長いURL・内部参照・本文を通常のready入力から除外／工程別factIdsで選択し省略した事実はID指定で追加参照／同じ記事の固定本文版・根拠・出典を復元／別日・別工程の参照を拒否／古い参照は閲覧できても新規成果物の確認には利用不可。

確認アプリは専用ポート18765と隔離DBで代表1記事だけ表示。ChromeのDOMと画像を確認し、要約・分類・人物・出典とProject DB表示を検証した。HTTP成功だけで表示確認済みにはしていない。最初の詳細画面URL組立てで列名誤りがあり、正しいarticle_identifiersのキーで修正した。これは未成功の試行として扱い、成功した詳細画面を証跡に保存した。

変更したPython 12ファイルの構文、git diff --check、生成指示元JavaScriptの構文を確認。全リポジトリの試験・本番DB全件照合・DB集約の再検証は実行していない。

### 初回入力＋追加参照の比較

同じ合成記事、同じ元ルール、同じ開始保存/保留状態を使用。共通確認は記録なし、要約・人材・分類はreadyだが成果物未保存。追加参照は全工程で同じ保存本文の文字範囲6000〜7000（末尾で切り詰め）。DB内で検索しただけの情報はAI入力量に含めない。

以下はコンパクトJSONのUTF-8バイト数÷4を入力ごとに切り上げた**推定トークン**。文字数とバイト数は実際の出力から測定した。トークナイザー算定・セッションの実使用量ではない。

| 工程 | 変更前：初回＋追加＝合計 | 変更後：初回＋追加＝合計 | 削減率 |
| --- | ---: | ---: | ---: |
| 共通本文確認 | 5129 + 783 = 5912 | 4623 + 450 = 5073 | 14.19% |
| 要約 | 906 + 1091 = 1997 | 204 + 450 = 654 | 67.25% |
| 人材確認 | 935 + 1098 = 2033 | 203 + 450 = 653 | 67.88% |
| 分類 | 1009 + 1093 = 2102 | 206 + 450 = 656 | 68.79% |
| 合計 | 12,044 | 7,036 | 41.58% |

実測データ量：30,637→11,246 Unicode文字、48,167→28,128 UTF-8バイト。入力データ比較だけの結果であり、タスク全体・キャッシュ・課金量の削減率ではない。実タスクログ未接続のため実使用量は未計測。他タスクの計測状態は流用していない。

## 残る制限・未実施

- 実セッションの入力/キャッシュ入力/課金使用量は未計測。他タスクのログ・計測状態は流用していない。
- 推定トークンは入力データのUTF-8バイト数÷4の切り上げ。モデル固有トークナイザーではなく、日本語やJSONの実トークン数を保証しない。固定ルール・会話・ツール外枠は比較対象外。
- 旧保留記録にmissingTopicsがない場合は関連性を推測せず保留維持。自動再開はDBに保存された明示的なtopic付き事実・引用の追加に限定。
- 共通確認のない旧成果物・記事との対応が証明できない記録・空の提案は保存完了と推測しない。
- 稼働中n8nへのワークフロー定義同期と日次運用全体の実行は未実施。ローカル生成指示元は更新済み。次回の稼働定義更新時に既存の同期手順を使用する。
- 本番DB全記事検証・DB集約の再検証・外部ページ再取得は対象外として未実施。代表データのみで確認。

## 記録と再開地点

- 詳細結果：docs/ai-input-minimization-verification.json
- 比較材料・画面証跡：.operation-state/ai-input-minimization/（合成データのみ）
- 再実行用の代表例：tests/test_ai_input_minimization.py、tests/ai_minimization_fixture.py
- この実装・代表検証の残作業なし。稼働定義への同期は未実施として分離。運用開始時は保存済み成果物を再生成しない。


## 追加対応：稼働反映と旧保留の再開（完了）

実装済み：旧保留の関連性判定・別履歴への判定記録。稼働中n8nへ反映済み：入力最小化の生成指示。初回の代表6パターンと入力量比較はそのまま保持した。

### n8nの同期と稼働版確認

- 対象：Daily Keyword News Summary、workflow ID UHJ4LW3Oswhpj4zV。
- 対象ノード：Build Markdown Files、node ID dce1c6b9-d4d7-498a-8b8c-cfbcf2e917fb。
- 同期前の構造差分は対象ノードのparameters.jsCode 1箇所だけ。収集条件・実行時刻・認証参照・接続を含むその他ノードの差分なし。
- 既存sync_workflow_to_n8n.pyを --active-state preserve で使用し、PUTとactivateを各1回実行。再送・日次運用実行なし。
- 旧版：d47524a4-208b-4f8f-b2a7-caf499d2f76e。
- 公開された稼働版：6bb0c4c3-ebc3-453d-a88c-b63a332ffc03。
- GET /api/v1/workflows/UHJ4LW3Oswhpj4zV の active=true、activeVersionId=versionId=activeVersion.versionId と、activeVersion.nodes内のコードがローカルの新しい入力・追加参照の指示に完全一致することを確認した。下書き保存だけの確認ではない。
- n8nは同期時に callerPolicy=workflowsFromSameOwner と availableInMCP=false を補完した。最初の厳密一致確認ではこの2項目で停止したため、更新を再送せず差分だけを読み取った。インストール済みn8nの未設定時の既定値、実行プロセスにcallerPolicy上書きがないこと、MCPはtrueのみ許可する実装を確認し、実効設定は不変と判断した。既存settingsの各値・staticData・接続・対象コード以外の全ノード情報は維持した。
- 証跡：.operation-state/ai-input-minimization/followup/n8n-deployment.json。復旧用の定義コピーは同フォルダーの.private.json（0600）に保存し、認証情報を報告ファイルへ複製していない。

### 旧保留の再開条件と保存

scripts/legacy_hold_relevance.py を追加し、共通のhold_stateと既存レビュー保存経路へ接続した。missingTopicsなしだけを理由に恒久保留しない。対象工程の不足理由、当時の根拠付き人物、事実・引用から対象と不足項目を一意に対応付ける。対応できる項目は所属、契約期間、契約開始日、開催・出演日、参加費等。元と新しいreview_input_snapshotsで引用と本文版を確認する。

新しい引用から確認できる対象人物・不足項目・値の組合せを比較し、未使用の関連材料が増えた時だけ未完了工程を再開する。日付・ルール・ハッシュ・fact ID・文章の言い換えや、無関係な設備情報などの追加では再開しない。再保留が保存されたらその判断を新しい基準とし、同じ材料での再開を防ぐ。保存済み工程は従来どおり先に除外する。

判定不能なら不足項目の対応不可、対象人物の曖昧さ、根拠検証不能、未確定/未対応表現などの理由を記録して保留する。補助判定は既存immutable historyのai-legacy-hold-assessmentに、元保留review ID・今回review ID・各本文版・判定条件・該当fact/evidence ID・判定理由として追記する。元の保留判断・本文snapshot・引用は上書きしない。繰り返し読み取りでは同一補助履歴を重複作成しない。

必要な日付/工程の入力を読むとき、または対象記事へ新しい確認材料を保存するときだけ判定する。旧全件のAI再確認や一括補完は実行していない。再開可否のために本文全体をAIへ返さず、本文の自動再取得も行わない。

### 指定された代表1記事の確認

tests/test_legacy_hold_reopening.py の1テスト内で、隔離SQLiteの同一記事について次の4状態を確認した。

| 状態 | 結果 |
| --- | --- |
| 材料追加なし（時刻・ルール・言い換え変更を含む） | 保留維持：合格 |
| 無関係な会場設備の根拠追加 | 保留維持：合格 |
| 旧理由「星野アキの契約期間が不明」に対応する契約期間の引用追加 | 人材工程だけ再開：合格 |
| 再開後も不足して新たに保留、その後も同じ材料 | 保留維持・再開なし：合格 |

同じ記事の保存済み要約の除外、分類保留の維持、判定不能理由の別記録、過去のreview/snapshotの不変、取得履歴1件・保存済み要約1件の維持も確認した。取得・成果物再生成は行っていない。曖昧な人物対応と判定不能の説明を補強した後はこの1記事テストだけ再確認し、従来6パターンの一括再検証はしなかった。

通常のready入力と本文追加参照は、修正前モジュールと同じデータ・状態でJSONが完全一致した。形式・データ量は変更していないため、既存の工程別入力量比較を更新せず保持した。新しい実使用量計測や削減率の主張はない。

### 現在残る制限

- 自由文全般を理解するAI分類器ではない。過去理由/対象/根拠が曖昧、認識できない表記、未確定の引用、snapshot不足は説明付きで保留する。missingTopicsがないだけで停止する制限は解消した。
- 今回の動作確認は指定4状態の代表1記事に限定。他の認識表現の網羅確認は未実施。
- n8nは公開された稼働定義で反映を確認した。日次運用全体・全件収集・保存済み成果物再生成は依頼範囲外として未実施。
- 実セッション使用量は引き続き未計測。初回の入力データ比較と区別する。

### 記録

docs/ai-input-minimization-verification.json の followUps に同期識別情報、公開版確認の根拠、限定テスト結果を追加した。初回patternsとinputComparisonは変更せず、解消した初回制限もsupersededLimitationsに保持した。今回の未完了作業なし。既存の未コミット変更を維持し、コミット・プッシュなし。

## 完了後の整理（2026-09-17）

利用者の依頼に基づき、追加対応を含む完了記録を確認し、今回の実装に属する変更をコミットするため整理した。稼働中n8nへの反映・旧保留の再開対応に未完了事項はない。実使用量の未計測等、記載済みの測定上の制限は維持する。

- 今回の変更前コード5ファイル、追加対応前のコード・文書3ファイル、ワークフロー定義コピー3ファイルを削除した。
- 変更前の隔離検証DB・ルールコピー（fixture-before）と、追加対応前コードのキャッシュ（followup/__pycache__）も削除した。合計21ファイル、1,956,098バイト。
- 比較用の変更前後入力、入力量比較結果、着手前状態の記録、画面証跡、稼働版の確認結果、旧保留の検証結果は保持した。本番DB・収集記事・他作業のバックアップは変更していない。
- 上記の古い記録に登場する復旧用コード・定義コピーは、作業時点には存在したが、この整理で削除済み。既存の検証結果を書き換えて再実施済みにはしていない。
- 旧保留テストの任意の変更前モジュール比較は、今後コピーなしで再実行すると未実施として記録される。今回の合格証跡はそのまま保持する。
- コミット対象は入力最小化の実装・テスト・運用手順・決定事項・指示書・完了記録。着手前からある日次データ等の無関係な変更は対象外。プッシュは行わない。

削除したパスと保持した証跡は検証JSONのpostCompletionCleanupに記録した。全件検証・代表6パターンの再実行は行わず、コミット対象の差分と記録の整合性を確認した。

## 2026-09-17: 日次レコードと本文の読込みを分離

- Reader.load_day_articles は収集情報と記事情報だけを読み、load_day_captures は本文を読む。従来の load_day は両者を組み合わせる互換入口として維持した。
- load_day の capture_urls は None なら全件、空集合なら本文なし、URL集合なら一致する本文だけを取得する。本文を含み得る source_records.raw_json も、本文一覧を調べる段階では取得しない。
- AI入力の URL 指定では対象記事の本文だけを取得する。保存済み状態の一覧では日次本文の一括取得を行わない（保存済みレビューの検証に必要なスナップショット読込みは維持）。
- ポリシーハッシュは入力生成・選定件数集計の各呼出し内で一度計算し、レビュー検証と参照情報に同じ値を使用する。
- URL未指定時は、レビュー検証に本文が必要なため、ページ分割前の本文取得を維持する。本文整合性の検証を省略する変更ではない。
- 分離した一時DBによる test_ai_input_loading で、対象本文だけの取得、空集合・未知URL、保存済み一覧、入力内容一致、ポリシーの計算回数、本文変更後の再レビュー判定を検証。

## 2026-09-17: AI入力組立ての責務を分離

- build_payload から対象選定（_select_input_candidates）、記事表示と参照情報の整形（_build_input_article）、参照情報・保留評価の保存（_save_input_references）を分離した。
- 候補選定後にページ分割し、読取りトランザクションを閉じてから保存する順序を維持。公開関数の引数・JSON形式・参照ID生成・既存のレビュー検証を変更していない。
- 中間変数を selection、capture_references、latest_fetch など役割が分かる名称にした。
- 関連テスト39件が成功。追加テストでは複数ページの順序と総件数、末尾、不正指定時の保存防止、レビュー済み記事への本文追加を確認した。
- 一時DBで変更前後を72条件（3タスク、レビュー前後、通常・URL指定・本文続き・ページ境界・不正引数）比較し、出力・例外メッセージ・保存要求の一致を確認した。比較時の保存要求はモックで捕捉した。

## 2026-09-17: 記事選定と保留解除の判定を読みやすく整理

- select の保存済み、本文の同一記事確認、保留、本文取得不可、レビュー検証という判定順序を維持し、completion_receipt、latest_review、review_is_valid など役割を示す変数名に整理した。
- hold_state は直近の保留と不足トピックを明示し、現在と保留時の根拠の差分を段階的に計算する。トピックを持たない過去の保留は従来の評価処理を使用する。
- material は対象トピックの事実と引用からハッシュを作る手順を明示。根拠IDの変更だけでは新しい材料として扱わない。
- 関連テスト46件が成功。新規テストで判定の優先順位、ポリシー変更、記事の取り違え、根拠IDと引用の変更を確認した。
- 一時DB上で変更前後を120条件（新規・レビュー済み・保留・再開、3タスク、本文状態・本文なし・別記事、現行／変更ポリシー）比較し、結果・例外・保留評価の一致を確認した。

## 2026-09-17: 保存済み判定の根拠を分離

- completed は完了記録、保存された成果物、過去の提案の順に確認する入口として整理した。成果物の確認は _completed_artifact、提案の確認は _completed_proposal に分離。
- _proposal_matches_article は旧記事キー、または提案日とURLから一意に特定できる記事を照合する。valid_review は保存当時の入力スナップショットとルールでレビューを検証する。
- 公開関数の引数・戻り値、レビュー検証、根拠の部分一致条件、既存の検索順序を維持した。レビュー済みという状態だけで完了を推測しない。
- 関連テスト52件が成功。新規テストでは成果物へのフォールバック、完了記録の優先、過去の提案と記事の照合、根拠なし・非AI・無関係な提案の除外を確認した。
- 一時DBを使う13テストで変更前後の完了判定を同時比較し、165回の呼出しですべて一致した。完了記録の優先順位テストでは両実装に同じテスト用履歴を渡した。

## 2026-09-17: 参照IDからの記事・本文・レビュー復元を整理

- resolve は参照ID・日付・タスク・記事の検証を担当し、本文復元を _resolve_reference_capture、レビュー復元と根拠検証を _resolve_reference_review に分離した。
- 公開関数の引数・戻り値、検証順序、例外メッセージを維持。参照に記録された取得履歴と本文版を使用し、その後の最新データへの置換は行わない。
- 関連テスト58件が成功。新規テストでは正常な復元、レビュー前の参照、日付・タスク違い、欠落した参照先、本文版不一致、本文・レビューなし、本文更新後の旧参照を検証した。
- 一時DBを使う13テストで変更前後を直接比較し、53回の参照復元について戻り値または例外の型・メッセージが一致した。

## 2026-09-17: 追加情報の状態確認と出力整形を分離

- additional は参照の復元と現在の保存済み・保留状態の確認を担当し、本文・詳細・出典・事実・根拠の整形を _additional_reference_payload に分離した。
- 参照に記録されたレビューと現在のレビューを区別する変数名に整理。公開引数、出力形式、IDの検証、保存済み・保留を先に返す順序を維持した。
- 関連テスト64件が成功。追加した6テストで本文の部分取得、レビュー有無による詳細の違い、出典、不明IDの拒否、本文なし、保存済み・保留の優先を確認した。
- 一時DBを使う19テストで変更前後を直接比較し、追加取得35回について戻り値または例外の型・メッセージが一致した。

## 2026-09-17: レビュー補完と完了記録の保存前検証を整理

- expand_review は参照から補完する出典フィールドを source_fields にまとめ、入力との不一致を検証する流れを明示した。参照のないレビューの扱いと、入力を書き換えない動作を維持。
- completion はレビュー済み根拠の確認、同一レビューでの完了確認、現在の入力との照合、完了記録の保存という順序に整理。現在の記事・本文・ルールとの照合は _validate_current_completion_inputs に分離した。
- 関連テスト71件が成功。追加7テストでフィールド補完、不一致の拒否、完了記録の内容、未レビュー・ルール変更時の保存防止、保存済みレビューの扱いを確認した。
- 一時DBを使う14テストで変更前後を直接比較し、レビュー補完30回・完了処理13回の戻り値・例外・完了記録の保存要求が一致した。比較用の旧完了処理では保存をモックで捕捉し、新処理のみ既存のテスト用保存先へ渡した。

## 2026-09-17: 要約・提案の保存形式への変換を分離

- expand_artifact は種類別の入口として整理し、要約の文章化を _expand_summary_text、提案の参照解決を _expand_proposal_document、既存提案との結合を _merge_proposal_records に分離した。
- 提案の記事キーを取得する重複処理は _proposal_article_key にまとめた。既存キーが取得できない場合のハッシュによる代替を維持。
- 入力の複製、参照検証、要約の出力形式、既存レコードの保持、同じキーの更新と並び順、既存の例外条件を維持した。
- 既存の関連テスト71件と新規7件が成功。新規テストでは入力非変更、保存済み要約の保持、記事・関連付けの参照解決、URL不一致、5種類のコレクションの結合順序、キー不足を検証した。
- 一時DBを使う14テストで変更前後を直接比較し、保存形式への変換17回について戻り値または例外の型・メッセージが一致した。

## 2026-09-18: 提案結果と完了記録の対応を整理

- record_artifact から提案結果の確認と完了処理への引き渡しを _record_proposal_completions に分離した。confirmed では対応する記事の提案を必須とし、none では参照を明示した否定結果を受け付ける既存条件を維持した。
- validate_proposal は必須コレクション、日付・版の確認、業務データ検証の順序を明示し、変数名を役割に合わせた。
- 関連テスト97件が成功。新規6テストで確認済み・該当なし、記事の欠落・不一致、不正な結果、空の提案、異なるタスクの参照を検証した。
- 一時DBを使う26テストで、変更前後の完了処理への引き渡し25回と提案検証6回を比較し一致を確認した。比較時は両実装の completion 呼出しをモックで捕捉し、比較後に新実装から実際のテスト用完了処理を一度だけ実行した。

## 2026-09-18: 残り5回の完了計画と第1回の結果

対象はAI入力まわりと関連DB処理。日次出力・レポート生成などへ範囲を拡張しない。

1. **完了**: 事実・根拠の選択処理を整理。
2. **完了**: 履歴保存・保留評価を整理。
3. **完了**: AI入力モジュールを責務別に分割。
4. 未着手: 関連するDB読取り・書込み処理を整理。
5. 未着手: 全体の回帰確認、必要な修正、作業記録の整備。

第1回では task_facts と packet の選択条件を selected_fact_ids、selected_evidence_ids、linked_fact_ids など役割が明確な変数で表現した。明示された空の選択、人物に根拠がない場合の全事実へのフォールバック、元の並び順、topics の出力除外、入力非変更を維持した。

- 新規8テストを含む関連105テストが成功。
- 事実と根拠の並び、人物の有無、明示された選択、3タスクを組み合わせた1,944条件で変更前後の選択結果・出力・入力非変更が一致。
- 残り4回。次は履歴保存・保留評価の整理。


## 2026-09-18: 完了計画の第2回 — 履歴保存・保留評価

- history、save_history、build_references の処理を読みやすく整理し、履歴キー・履歴ID・出典ID・保存データの関係を明示した。公開引数と識別子生成規則、保存順序は維持。
- save_hold_assessment は保存済み判定、同じ記事に属するレビューの確認、過去の保留状態の確認、根拠に基づく再計算、履歴保存という順序を明示した。
- record_legacy_hold_assessments は対象記事の未完了タスクだけを評価する従来の範囲を維持し、評価結果を表す変数名を整理した。
- 新規5テストを含む関連110テストが成功。履歴IDと出典、入力非変更、参照先欠落、保留でないレビュー、不一致評価の保存防止を確認。
- 一時DBを使う12テストで旧新の保留評価保存を比較し、29回の呼出しで結果・例外・履歴保存要求が一致した。比較では双方の保存要求を捕捉し、その後に新実装を実際のテスト用保存先へ一度だけ実行した。
- **5回中2回完了、残り3回。** 次はAI入力モジュールを責務別に分割する。


## 2026-09-18: 完了計画の第3回 — 責務別のモジュール分割

| モジュール | 担当 |
| --- | --- |
| scripts/ai_input_facts.py | タスクごとの事実・根拠・人物の選択と保留解除材料の比較用ハッシュ |
| scripts/ai_input_references.py | 参照IDの生成、記事・本文・レビューの復元と検証 |
| scripts/ai_artifact_expansion.py | 要約の文章化、提案の参照解決と既存レコードの結合 |
| scripts/ai_input_minimization.py | 既存の呼び出し口、記事選定、完了・保留の判定、保存処理の調整 |

- 既存の関数は ai_input_minimization から再エクスポートし、CLI、DB書込み処理、既存テストの呼び出し方を維持した。変換モジュールは参照モジュールに依存し、分割先から調整用モジュールへは依存させない。
- 3ファイルへ12関数を移動。ai_input_minimization は692行となった。分割前後の全38関数の構文木を比較し、処理内容に変更がないことを確認した。
- 関連110テストが成功。新しいPythonプロセスでCLI・DB書込み・呼び出し口の読込み順6通りと分割先3モジュールの独立インポートを確認し、計9チェックが成功。
- **5回中3回完了、残り2回。** 次はAI入力に関連するDB読取り・書込み処理を整理する。日次出力・レポート生成などへ範囲を広げない。
