# AI Talent Index Instructions

Each daily workflow run writes an instruction Markdown file here. It points to
the structured records for that day and defines the evidence, pending-status,
and output requirements for AI-assisted talent/article indexing.

The instruction does not update n8n Data Tables. Create a reviewed proposal in
`content/talent-index-proposals/` before applying it through the dedicated
n8n workflow.