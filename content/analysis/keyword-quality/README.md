# Keyword Quality Reports

Each `keyword-quality-YYYY-Www.md` file evaluates saved search keywords against the archived titles and excerpts. It also reports noise candidates and aggregates the decisions in `../../ai-keyword-candidates/`.

The report never changes the n8n default keyword list automatically.