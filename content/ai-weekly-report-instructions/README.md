# AI Weekly Report Instructions

n8n writes one instruction file after each daily news collection.

The filename is the Monday (JST) for the relevant week. Follow the newest
instruction together with the other daily AI instruction files, then update
the matching file in content/weekly-reports. The file is overwritten during
the same week so the instruction describes the current collection range.
