# 2026-09-10 運用結果

- n8n日次定義をプロジェクトの最新版へ同期済み。active、JST毎日08:00、定義一致を確認。収集実行159成功、627記事。反映実行160（記事・人物）、後続の分類反映は成功し、実DBとの全保存フィールド一致を検証。
- 全627記事の共通レビューを保存。本文確認済み20件の日本語要約と分類を作成、本文不足607件は個別理由付き保留。
- 記事627件、人物87件、関係198件、分類20件をDBへ反映（新規挿入数ではなく反映対象数）。新規人物16件はpending・search_enabled=false。既存人物の承認・検索設定・別名を維持。
- キーワード7候補、固定監視追加推奨3語を保存。検索設定の変更なし。
- 2026-09-07週の週次考察・自動数値を保存・検証。採否スナップショット反映済み。9/7〜9は入力欠損のため部分レポート。旧分類を含む集計28件と本日レビュー20件を区別し、入力ハッシュ未付与の警告を保持。
- http://127.0.0.1:8765/ は稼働。独立プロファイルのヘッドレスChromeでDB表示・記事の当日要約と分類・人物関係・今週レポートを実描画確認。証跡: `.operation-logs/display-2026-09-10/verification.md`。

## 次回の工程保留を防ぐ修正

1. 収集Webhookの終了枝が異なっても、一意な成功実行と生成ファイルの一致から完了を判定。実行詳細のみ読取上限256MiB。他応答は32MiB。
2. HTTP 429後は同じ取得プロセス内で同一ホストへの追加リクエストを止め、他のホストを継続。未試行と本文不足を記事ごとに記録する。
3. n8nがSQLiteへ保存するUTC・ミリ秒精度の日時を正規化して照合。同一時刻の表記差を誤検出しない。送信済みの提案は再送せずDB照合で完了させる。
4. autoarticle-operationsスキルと運用文書に、全工程の依頼では記事単位の保留があってもレビュー可能な提案の反映まで継続することを明記。根拠不足をreadyに変換しない。
5. 要約成果物を確認アプリの読込形式に合わせ、当日20件の読込一致を確認。

検証: 運用37件、日時照合3件、429取得制限1件のテスト成功。git diff --check成功。Gitコミットは行っていない。

## 工程別トークン

このタスク `01a08a01-d35a-7f32-84d5-63659a8a347f` のログへ接続。`content/operation-usage/2026-09-10.md` とJSONに観測差分を保存。
共通の本文・人物・分類判断は「共通本文確認」、各提案の共通レンダリングは「記事要約」の計測区間に含まれる。人材・分類のcheckpoint区間には独立した使用量通知がなく、個別値は未計測。画面検証中の要約形式修正・週次数値再同期は「画面確認」に含まれる。未割当量を恣意的に按分せず、自動レポートに計算根拠を保持。最終返答はfinish後のため含まれない。
