# フェーズ5 継続同期（完了・業務経路はlegacy）

2026-09-17更新：利用者の指示でD:/N8Nのバックアップ・検証コピーを削除し、必要な登録済み根拠だけをプロジェクト内へ移した。この資料のD側パスは作業当時の記録であり、現在の復旧先ではない。[保存先整理記録](database-storage-cleanup.md)を参照。

## 現在適用する運用・受入条件（2026-09-16改訂）

フェーズ5は f1504b1 で完了済み。005・006を適用し、通常収集182と通常要約 phase5-summary-20260916-v4 を同期済み。フェーズ6は未開始。

今後の検証は [検証・完了条件の現行基準](database-validation-policy.md) と [作業指示書](database-consolidation-work-instructions.md) に従う。代表パターンは原則各1件、必要な境界例のみ最大3件。全件照合・全テーブルハッシュ・過去判断全再現・既存13件全照合を追加の完了条件にしない。過去に成功した取消し・再送・復元・履歴試験は、関連する処理を変更しない限り再利用する。

### 今後の同期

1. 旧経路で実際に完了した業務と完了記録を確認する。成功済みの業務を検証回数のために再実行しない。
2. 安定した処理IDと保存対象を確認して同期を準備する。旧要求と証跡は保持する。
3. 必要な復旧地点を確保して同期する。実行時の入力・参照・トランザクションの保護は維持するが、受入のための全件検査を別途重ねない。
4. 関係する代表例の値・記事対応・状態・履歴・出典と、処理の成否・件数を確認する。
5. 業務影響のない差分は理由を短く記録して続行する。ハッシュ差だけをフェーズ停止にしない。
6. 実際の欠損・誤対応・重複・部分保存・結果不明などは該当要求を止め、原因が分かる修正は作業範囲内で行って代表試験後に再開する。

管理操作は scripts/autoarticle_db.py の prepare-sync、sync、sync-status を使う。結果不明時は状態確認を優先し、内容を変えず同じ要求で再開する。同じIDに異なる内容を通してはならない。未登録の旧要求から同じ業務の要求を作り直す場合だけ、supersedes の既存契約を使う。

初回判断・親競合と再評価・追加キャプチャ参照は分離済み。旧履歴を保持し、新しい観測だけで採用判断・記事対応・保留状態を変えない。

既存ツールが無害なハッシュ差で拒否する場合は、代表例で業務影響を確認して比較／準備処理を限定修正できる。この文書改訂自体はコードの拒否条件を変更したり、未実行の同期を成功にしたりするものではない。新しい全件比較や利用者の再承認待ちを自動的に追加しない。

完了結果は [検証JSON](database-phase5-verification.json)、履歴は [進捗資料](database-consolidation-progress.md) を参照する。

## 過去の実装・中断・完了記録

以下は記録当時の経緯であり、今後の作業条件ではない。「全件必須」「本番使用不可」「未完了」「差分で停止」などの過去指示を再適用しない。現行条件は上記と現行基準を優先する。

<details>
<summary>フェーズ5完了までの保存記録</summary>

最新状態: 005・006適用と通常要約の本番同期が成功。ユーザー承認の42パターン／121代表行、本文欠落13件、再送追加0件、取消し／再開、同期状態を含む復元を確認し、フェーズ5を完了。以降の中断・未完了の記述は各時点の経緯であり、最新結果は末尾と検証JSONを参照。

2026-09-16 02:18 UTC現在、隔離受入と004適用に続き、実際の通常収集182（464記事）の本番同期1回が成功。取消し・同一ID再開・再送0件、独立照合差分0件、同期状態を含む復元を確認した。異なる通常業務の複数回同期と他対象の本番受入が残るためフェーズ5は未完了。フェーズ4確定点は `734eee2752e16fbd13db3ab8f873ffe426a1ab32`。以下は経緯を保持しており、最新結果は末尾と検証JSONを参照する。

## 作成済みの範囲

- `004_continuous_sync.sql`: 同期結果区分、再送回数、試行記録、変更前後と出典、旧経路の完了証跡。既存マイグレーション001～003は変更しない。
- `continuous_database_sync.py`: 一つの処理ID・要求ハッシュで、一つの業務トランザクションを保存する。異なる内容のID再使用は競合として停止。失敗時は業務更新を取り消し、失敗記録だけを別トランザクションで保存する。同じIDの成功再送は同じ結果を返し、行を追加せず再送回数だけ更新する。
- `legacy_sync_projection.py`: 保持したスナップショットから隔離DBを作り、変更前提条件と差分計画を生成する。旧行・元キー・元行ハッシュを監査履歴に残す。既存の不変履歴の上書きや保留の格上げは停止する。削除された別名・副カテゴリの履歴方針も未解決のため停止する。
- `prepare_legacy_sync.py`: 既存運用の完了状態を再確認し、完了証跡と入力を保存する。対象処理IDが一致し、入力が変わらない場合だけ同期できるようにする。
- 管理CLIに `prepare-sync`、`sync`、`sync-status` を追加した。自動実行フック・新しいDBサービスは有効にしていない。

## 中断した検証

業務入力がフェーズ4と同じ状態で隔離DBを再生成したところ、レビュー1,538件が `current` から `needs_review` に変わり、対応する移行元レコード1,538件が理由付き保留となり、新しい競合1,538件が生成された。計4,614行の差分を許容していない。

原因は、入力取得が業務データ8ディレクトリだけを対象とし、`article_review_facts.POLICY_FILES` のルールファイルをスナップショットへ含めていないこと。旧経路とフェーズ3復旧点のルールハッシュは一致するが、今回の隔離入力では `policy_hash` がNULLになる。ルールや旧データ自体の変更ではない。

中止指示に従い、原因確認と記録だけを行い、ルールファイル追加による再実行や本番への適用はしていない。46件の小規模回帰テスト成功を、実データ受入成功とは扱わない。通常の日次運用の複数回実施も未完了。

## 再開時に必要な対応

1. 同期準備の入力台帳にルール依存ファイルと実装版を含める。欠落した場合は比較・取込前に停止し、現在の作業ツリーへ暗黙にフォールバックしない。
2. ルールハッシュの再現と欠落拒否を回帰テストへ追加し、元の中断証跡を保持したまま新しい隔離候補を作る。
3. 同一入力の比較差分0件を確認する。本文欠落13件・既存競合18,305件・旧新対応を全件確認する。
4. 全対象の新規・更新・版追加・出典保存を、独立した旧経路との照合で受入確認する。下記コマンドを使用可能とするのは、その確認後。
5. 完全な同期前バックアップ・復元を確認してから004を適用する。旧業務処理完了後に `prepare-sync --operation-id ID --date YYYY-MM-DD --step STEP --output SNAPSHOT`、続いて `sync --operation-id ID --snapshot SNAPSHOT` を使う。結果不明時は `sync-status --operation-id ID` または同じ要求の再送だけを行い、新しいIDを作らない。
6. 通常の日次運用を複数回実施し、同期・照合・復元・停止時のlegacy単独運用を確認する。過去日付の再収集や合成テストを通常の日次実績に置き換えない。

コマンドは `python3 scripts/autoarticle_db.py --database DB_PATH` のサブコマンド。現在はドラフトであり、上記の問題を解決するまで本番DBには使用しない。同期を実行しなければ既存経路だけでの動作を維持する。読み取り切替・書き込み切替・自動書戻し・旧データ削除・本文補完・自動統合は含めない。

機械可読な記録は [database-phase5-verification.json](database-phase5-verification.json)、進捗は [database-consolidation-progress.md](database-consolidation-progress.md) を参照。


## 依存台帳修正後の状態（2026-09-15追記）

上記の規則依存不足は修正し、新規の完全スナップショットと別場所への復元を検証した。`sync_snapshot_dependencies.py` が規則・実装台帳を検証し、`complete_sync_snapshot.py` が取得前後の一致確認と完全一式の保存・復元を行う。失敗した取得先は隔離して保持し、既存スナップショットを修復して再利用しない。

依存修正後の全件比較では旧4,614差分は発生しなかった。一方、本文欠落13件の競合から、歴史的受入処理が保存した `acceptance_research` と `preserved_missing_body_claim` が欠落する差分を検出して中断した。投影結果への補記、既存競合への一律置換、許容差分への承認はしていない。詳細は進捗・検証JSONを参照。

本番使用不可の状態を継続する。004適用と同期の前に、この調査証跡を再現可能に保持する扱いを確定し、未承認差分0件、再実行0行・全論理ハッシュ一致、隔離DBのバックアップ復元を順に確認する。その後にのみ本番受入と複数の実日次運用へ進む。今回の隔離投影を実日次運用に数えない。

## 受入判断台帳による再生成（2026-09-15再開）

`docs/database-missing-body-decisions.json` に本文実体欠落13件の承認判断を保存する。本文は含めない。規則依存台帳へパス・形式版・サイズ・SHA-256を記録し、同期要求ハッシュに含める。欠落・改変・未登録判断・元行との不一致は投影開始前に拒否する。

`missing_body_acceptance.py` をフェーズ3受入と隔離投影で共用する。一括移行後、対象と保存値が一致する本文欠落競合だけに、承認済みの2項目を決定的に再生成する。再適用は更新0件でなければ停止する。現在の専用DBから競合JSONをコピーする処理や、比較対象から2項目を除外する処理は使わない。

バックアップ専用出力は単独ファイルとして保存するためDELETEジャーナルを使用する（`standalone-backup-v2-delete-journal`）。通常のDB接続はWALのまま。バックアップ作成・検証でも外部キーを有効にし、同期書き込みと整合性検証を省略しない。Dドライブへの出力を利用する。

実データでの全件比較・再実行・復元に合格するまで、本番への004適用と同期を開始しない。通常業務の実績は別途必要であり、隔離投影や合成試験を実績に数えない。

## 最新結果：実業務の本番受入待ち

回帰69試験、完全復旧344ファイルの作成・復元、隔離全件比較の未承認差分0件、受入注記13件の再現と再適用0件、同一投影の追加0件・全論理ハッシュ一致、候補DBのバックアップ復元に成功した。その後、本番へ004のみを適用し、既存業務データ不変、外部キー違反0件、整合性成功、legacy経路維持を確認した。

本番同期と通常業務実績は0件。実際に完了した業務の証跡を得てから、上記の同期準備と本番受入を行う。過去データ再生や合成試験を通常業務の回数へ算入しない。実際の同期状態を含む復元確認など未完了項目は検証JSONの `production_sync_cases` / `remaining` に記録している。フェーズ5完了・完了コミット・フェーズ6への移行は行わない。

今回の証跡：`/mnt/d/N8N/database/phase5-acceptance-20260915T090011Z`。本番004適用前の完全復旧一式を保持している。旧DB・旧業務ファイル・過去の復旧地点は変更していない。

## 実業務同期182の受入結果（2026-09-16）

処理ID `phase5-collect-UHJ4LW3Oswhpj4zV-182` で通常収集464記事を同期した。追加2,322行・更新0行。4行適用後の意図的失敗は全体取消しされ、同じIDで成功。独立照合9,288項目の差分0件、同一要求の再送は全テーブル追加0件（再送回数の監査更新のみ）。既存業務行・競合18,305件・本文欠落13件を保持し、新規464記事は一意性を推測せず理由付き保留とした。

実同期状態を含むバックアップ復元は全テーブル・スキーマの論理ハッシュ一致。依存内容だけを変更した同一IDの拒否は検証済み復元複製で確認し、本番の成功記録には注入していない。証跡は `/mnt/d/N8N/database/phase5-resume-20260916T003542Z`。復元複製の監査状態はその後の競合試験で変わっており、復元直後の一致記録と区別する。

通常業務実績は1回。追加の異なる完了業務、他対象の新規・更新・版の本番受入、差分状態記録、同期停止後のlegacy単独実運用が残る。次回は現入力と今回の最終基準を比較し、実際の完了証跡を固定して次の処理IDを準備する。処理182を再送して回数を増やさない。フェーズ5完了・完了コミット・フェーズ6への移行は行っていない。

## 追加の隔離受入（2026-09-16 02:26 UTC）

同期関連18試験に成功。全業務対象の追加、18行の新規版等と7行の更新、旧版・出典履歴保持、再送追加0件、独立照合差分時の全取消しと同一ID再開を確認した。証跡は `/mnt/d/N8N/database/phase5-remaining-20260916T022509Z`。本番全論理ハッシュと旧入力は不変。これらは合成入力の隔離検証であり、通常業務回数は1のまま。全対象の実業務による本番受入と、同期停止後のlegacy実運用は未完了。次の同期は追加テストを含めた新しい依存台帳・完全スナップショットから開始する。


## 通常要約同期の事前検証中断（2026-09-16）

9月16日の要約工程（7件ready・457件held）を旧経路で完了し、処理ID `phase5-summary-20260916-v1` の新規完全一式を復元検証した。隔離投影後、現在のキャッシュから旧判断を再計算した結果、既存の記事対応判断5件の `checks_json` を上書きする差分が発生し、`immutable_history_requires_new_record` で停止。本番同期の実行前であり、成功回数は1のまま。既存判断と依存根拠を保存する履歴化が必要で、差分承認や比較除外による対応はしない。

本番全論理ハッシュは実行182の成功・再送後と同一。FK違反0・整合性正常・全経路legacy・本文欠落13件維持を確認。同期停止中の実要約・キーワード工程完了によりlegacy単独運用は確認済み。証跡は `/mnt/d/N8N/database/normal-articles-20260916`。詳細は進捗資料と検証JSONの `normal_articles_20260916` を参照。フェーズ5は未完了であり、ここから本番syncコマンドを再送してはならない。


## 判断履歴の版管理と現在の再開条件（2026-09-16）

`005_identity_assessment_history.sql` は未適用。互換表を初回判断として残し、`identity_assessment_history.py` が登録済みの過去入力と保存実装から再現し、新しい根拠による評価を別版に保存する。`identity_history_dependencies.py` が `docs/database-identity-history.json` と過去根拠一式を検証する。根拠は元行への不変参照と実装実体としてDBにも保存し、復元後にDB内の根拠と保存コードから独立再計算できる。

現在の停止原因は `acceptance_conflict_not_unique`。旧本文行1843・2174に新キャプチャ参照由来の競合を追加すると、本文欠落元データ13件に対する競合が15件になる。既存競合を不変として、新しい観測根拠を別履歴に保存する扱いが未解決。新しい同期後チェックでも、この本文欠落受入条件を満たさなければ取消す。

再開時は、この未解決事項と全予定変更の独立意味照合を整えたうえで、新しい完全スナップショットを取得し、空候補DBで全件検証・再実行0件・復元を確認する。現在追加した照合は判断根拠と本文欠落の契約を強化するもので、その他全業務対象の独立意味照合を完了したことにはならない。

旧要求 `phase5-summary-20260916-v1` とハッシュ `c06a4451d5551f32d18b960fa8e7a322b609ff619e3af6685996a412dbbf5869` は保持し、変更しない。修正済み内容は別要求として、次の引数で旧要求との同一業務関係を記録する（今回は未実行）。

```text
prepare-sync --operation-id phase5-summary-20260916-v2 --date 2026-09-16 --step summary
  --supersedes-snapshot /mnt/d/N8N/database/normal-articles-20260916/summary-snapshot
  --supersedes-request-hash c06a4451d5551f32d18b960fa8e7a322b609ff619e3af6685996a412dbbf5869
  --output 新しいDドライブ上のスナップショット先
```

旧IDがDBに未登録であることとcheckpointのentry_hash一致を確認し、来歴を要求ハッシュへ含める。旧IDに状態があれば、新IDへの付け替えを拒否する。再要求・再送・隔離試験は通常業務回数へ含めない。

本番005適用は隔離全件受入・実データ復元・直前の入力／書き込み確認と完全バックアップの後だけ。本文欠落13件、既存競合、レビュー、要約、本文版、出典と全旧判断の保持を確認する。本文元行自体の更新等で初回対象が現在集合から外れるケースは現実装では停止するため、その更新受入も未完了項目として扱う。フェーズ5全条件を満たすまで完了コミットとフェーズ6へ進まない。

## 本文欠落参照履歴の修正（2026-09-16、受入検証中）

`missing_body_capture_history.py` は初回競合を保存済み入力から再現し、追加参照を006の履歴へ保存する。`missing_body_history_worker.py` は保存実装だけを隔離実行する。`compare_sync_semantics.py` は現在入力と登録済み過去入力から元行を引き直して、取込単位、本文、レビュー・引用、要約、収集、人材・分類・評価、出典、既存競合と新規保留理由を比較する。

判断履歴005と既存マイグレーションは保持する。本文欠落競合の一意性を緩和せず、既存競合をコピーして合わせない。追加参照は親競合・元データ・キャプチャ元行の不変参照と結び付け、範囲のハッシュと処理版を保存する。受入注記を確定した後で参照集合を保存し、それ以降の親競合更新を拒否する。

新要求 `phase5-summary-20260916-v2` は旧 `v1` と同じ要約工程の再準備として扱い、成功するまで通常業務同期回数は1のまま。旧要求・旧候補・中断証跡は保持する。新しい保存先は `/mnt/d/N8N/database/phase5-capture-history-20260916T044305Z`。現在は完全スナップショットと復元の検証中であり、005・006は本番未適用。

### 継続する元行更新と依存の固定（今回の追記）

元行が更新されて過去の source_record_id が現在入力から外れる場合は、登録済み過去入力と、ハッシュで固定した前回DBの不変な根拠から過去版を再現する。以前の判断結果をそのまま正解としてコピーしない。保存済み実装で判断を再評価し、全項目が一致した根拠・初回判断・各判断版だけを引き継ぐ。キャプチャの過去観測集合も元行から再計算して引き継ぐ。再現できない競合や根拠は停止し、制約を無効化しない。

完全スナップショットの project.sqlite のSHA-256を台帳と同期要求ハッシュへ追加した。欠落・改変を投影前に拒否する。元行の連続更新とキャプチャ内容の連続変更を小規模DBで検証し、旧元行・初回判断・根拠・観測集合を保持できた。既存001～005は変更していない。

最終回帰は111件成功。実データ用のv2/v3準備物は保持し、依存の閉じた範囲を補強したv4で受入をやり直している。v3はスナップショット公開後、復元の途中で意図的に停止した。I/O障害ではなく、本番書き込みもない。旧記録の公開状況の訂正は observed-stop-outcome.json に追記した。v1～v4はいずれも同じ通常要約の準備・再要求であり、通常業務回数へ重複加算しない。


### 検証範囲の変更（2026-09-16、ユーザー指示）

「すべての項目を照合する必要はない」「パターンがある程度問題無ければ合格」との指示により、残る受入は新規・更新・版・状態・出典などの代表パターンで判定する。既知の差分を許容したり、履歴上書き禁止、一意な親競合、要求IDとハッシュ、原子的保存、復元可能性を緩めたりする変更ではない。

v4の空候補DBでは既に独立意味照合1,997,203項目、過去の初回判断14,822件と再評価14,822件、キャプチャ参照集合、本文欠落13件の競合内容を検証済みで、未承認差分は0件。同一投影の再実行は追加0件、全テーブル・スキーマの論理ハッシュも一致した。この結果を再利用し、以後の同期先・復元先は各パターンの代表行と本文欠落13件を照合する。未検査の全行が合格したとは記録しない。

残る同期試験は隔離した復元DBで行い、業務データ・根拠・参照履歴の一括取消し、同一ID再開、再送追加0件、異内容拒否、バックアップからの状態取得を確認する。停止中でWALが空のDBは、コピー前後のSHA-256と書換え時刻を検査する閉鎖時バックアップを使用できる。途中の従来バックアップも削除せず保持する。


## フェーズ5完了（2026-09-16、代表パターンの受入基準）

ユーザーが承認した代表パターンの基準でフェーズ5を完了した。フェーズ6には進んでいない。過去の中断記録、I/O障害時の保存物、旧要求、旧候補DB、途中のバックアップは保持している。

- 005の判断履歴を維持し、006で本文欠落の親競合とキャプチャ観測を分離した。既存13件の競合ID・全内容・調査注記は当時の根拠から一致し、初回183観測と追加2観測を26集合として保存した。元行1843・2174の参照追加で親競合は増えていない。
- 初回判断14,822件と新しい根拠による再評価14,822件を併存させた。元行の連続更新、参照の内容変更、根拠欠落・改変、重複競合、旧履歴の更新／削除拒否を含む回帰111件が成功した。
- 新しい完全復旧一式465ファイルの復元、空候補DBの独立意味照合1,997,203項目で未承認差分0件を確認した。候補DBの再投影は追加0件、全テーブル・スキーマの論理ハッシュ一致。既存の競合18,769件（フェーズ3の18,305件を含む）を保持した。
- 残りの同期先・復元先は42パターン／121代表行と本文欠落13件を照合した。全業務対象の新規・更新・版追加は隔離回帰で確認し、本番で全種類の更新が発生したとは扱っていない。今回の要約要求は全行追加である。
- 隔離DBで全要求212,627変更の最後に失敗を注入し、業務・根拠・履歴を一括取消し。同じ処理IDで安全に再開し、再送の追加0件、異内容拒否、失敗／成功監査、同期状態を含む復元が成功した。
- 本番の停止状態・入力・規則・実装・既存DB・復旧用バックアップを再確認して005・006を適用。完了済み通常要約を `phase5-summary-20260916-v4` として同期し、212,627行追加、既存行更新0件、代表照合差分0件、再送追加0件を確認した。旧v1要求は変更せず、同じ実要約に由来する再要求として関連付けた。
- 本番の外部キー違反0件、integrity_check成功。同期後バックアップを別DBへ復元し、業務・根拠・判断・観測・同期状態が一致した。本文欠落13件はheld／unverified／本文版NULL／本文ペイロード割当なしを維持した。
- 実業務の同期実績は通常収集182と通常要約の2回。準備v1～v4、再送、隔離試験、過去再生は回数に含めない。読み取り元・書き込み先は全機能legacy。既存データ削除、自動統合、保留格上げ、本文補完、AI入力最小化は行っていない。

証跡: `/mnt/d/N8N/database/phase5-capture-history-20260916T044305Z/v4/`。最終合否は `representative-sync-acceptance-v2.json` と `production-summary-sync.json`。従来の候補検証はバックアップ方式変更時に止めたため、その結果と復元結果を両記録で結び付けている。検証用BLOB記録の修正前エラーも保持している。

本番適用前後のバックアップと復元先: `/mnt/d/N8N/backups/phase5-summary-20260916-v4/`（Windows: `D:\N8N\backups\phase5-summary-20260916-v4`）。以後の同期も既存業務完了後だけに実施し、同一ID・異内容の拒否と未確認差分での停止を維持する。

終了時点でn8nとDBサービスは停止、未完了実行0件。今回の完了条件に未完了項目はない。起動スクリプト2件と通常の記事作業のファイルはフェーズ5コミットの対象外とする。

</details>
