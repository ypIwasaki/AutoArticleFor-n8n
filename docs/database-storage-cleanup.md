# DB作業データの保存先整理（2026-09-17）

状態: 完了。利用者の「バックアップは不要、参照も外す」という指示と、D:/N8N全体を整理して必要な根拠だけをプロジェクトへ移す選択に基づく。

## 削除したもの

- D:/N8N/backups：DB集約時の復旧コピー一式。
- D:/N8N/database：必要な登録済み根拠を移した後の、過去の検証DB、途中コピー、移動・検証資料。
- 上記を含むD:/N8Nディレクトリー全体。
- プロジェクト内のdata/backupsと、.operation-state/database系のDドライブ参照リンク（合計8本）。

.operation-state/databaseはリンクを解除し、プロジェクト内の通常ディレクトリーに置き換えた。他の旧フェーズ用リンクは削除した。今後Dドライブの有無を通常処理の前提にしない。

## プロジェクト内に保持した根拠

現在のdocs/database-identity-history.jsonが登録する1セットだけを保持した。

- 登録ID：0b219d4d4d07adfcf21834d9af395554eaa99894bbe005fe45ded1cf6d0c4d46
- 保存先：.operation-state/database/identity-origins/（上記IDのディレクトリー）
- 登録ファイル：101ファイル、589,020,204バイト。別途origin.jsonの管理台帳を保持。
- 用途：既存コードによる過去の判断根拠の検証・再現。通常の復旧用バックアップ一式は保持していない。
- 登録パス・登録ID・台帳のハッシュは変更していない。参照先がプロジェクト内の実ディレクトリーになったため、既存コードの変更は不要。

本番の専用DB、n8n稼働定義、収集記事、保存済み成果物には変更を加えていない。

## 保存方針の更新

完全バックアップを90日保持する旧方針は、今回削除したバックアップには適用しない。削除済みバックアップやD側の作業場所を再作成しない。必要な作業ファイルはプロジェクト内の.operation-state/databaseへ保存する。

プロジェクト内の旧JSON／JSONLについての従来の保持方針は、今回のDドライブ整理とは別であり変更していない。

過去の進捗・検証資料に残るDドライブのパスや復元合格記録は、作業当時の履歴として保持する。現在利用できる復旧先・再実行用ファイルとして参照しない。削除した旧フェーズの一時データを、完了済み検証の再実行だけのために復元しない。

## 確認範囲

- 登録されたorigin.jsonのハッシュと、移した各登録ファイルの存在・サイズを確認した。
- D:/N8Nの削除と、D側を向くリンクの解除を確認した。
- DB本文の全件確認、全ファイルの再ハッシュ、DB移行の再検証は行っていない。

機械可読の整理結果はdocs/database-phase8-verification.jsonのpostCompletionStorageCleanup、ローカルの実行記録は.operation-state/d-drive-cleanup-20260917.jsonを参照する。
